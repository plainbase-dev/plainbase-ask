services:
  bot:
    build: .
    platform: linux/arm64
    ports:
      - "3000:3000"
      - "80:80"
      - "443:443"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
